# Your actual money. Never commit these.
# Anchored with a leading slash so docs/index.html — the stripped copy — still ships.
/index.html
/NOTES.md
/GUIDE.md
/RESTYLE-PLAN.md
# Timestamped safety copies of the real ledger, taken before destructive edits.
/index.html.bak-*
# The leak-detection word list is itself the sensitive data.
/.leakwords
# Rendered previews and the engine dump they come from: both carry real balances.
# The generator, its stylesheets and preview-data.js hold no data and are tracked.
/preview-*.html
/preview-data.json
*.backup.json
money-backup-*.json
money-*.csv
